package org.optimizely.social.channels.youtube;

import org.optimizely.social.ChannelSDKActionImpl;
import org.optimizely.social.Channel;

public class YoutubeSDKAction extends ChannelSDKActionImpl {
    YoutubeSDKAction(Channel channel) {
        this.setChannel(channel);
    }
}
